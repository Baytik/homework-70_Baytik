import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <div className="logo">
                <h2>TV Shows</h2>
            </div>
        </header>
    );
};

export default Header;